
# O arquivo user-data que fica dentro da pasta packer-proxmox-ubuntu/ubuntu-22-template/ contempla o envio da chave publica da bastion do meu lab, para evitar digitar senha nas automacoes, para usar em produção basta comentar/remover  a linha que copia o ssh-key  
# a linha é a 47 -- >  Essa linha   - curtin in-target --target=/target -- bash -c "echo 'ssh-rsa 

